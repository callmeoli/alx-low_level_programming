#include "main.h"
/**
*_puts - prints a string followed by a new line
*@str: input
*/
void print_rev(char *s)
{
int i;
for (i = 0; s[i] != '\0'; i++)
{
}
while(i >= 0)
{
	_putchar(s[i]);
	i--;
}
	_putchar('\n');
}
